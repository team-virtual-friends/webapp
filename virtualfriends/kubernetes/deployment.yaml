apiVersion: apps/v1
kind: Deployment
metadata:
  name: virtual-friends-gke
spec:
  replicas: 1
  selector:
    matchLabels:
      app: virtual-friends
  template:
    metadata:
      labels:
        app: virtual-friends
    spec:
      containers:
      - name: virtual-friends-app
        # Replace $LOCATION with your Artifact Registry location (e.g., us-west1).
        # Replace $GCLOUD_PROJECT with your project ID.
        image: gcr.io/ysong-chat/flask-app:f622825-26be6f48
        # gcr.io/ysong-chat/flask-app:809f866-d6c44f0d
        # This app listens on port 8080 for web traffic by default.
        ports:
        - containerPort: 8080
        env:
          - name: PORT
            value: "8080"
        resources:
          requests:
            memory: "300Mi"
            cpu: "300m"
            ephemeral-storage: "2Gi"
          limits:
            memory: "1Gi"
            cpu: "1"
            ephemeral-storage: "4Gi"
---