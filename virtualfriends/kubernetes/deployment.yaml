apiVersion: apps/v1
kind: Deployment
metadata:
  name: virtual-friends-gke
spec:
  replicas: 1
  selector:
    matchLabels:
      app: virtual-friends
  template:
    metadata:
      labels:
        app: virtual-friends
    spec:
      containers:
      - name: virtual-friends-app
        image: gcr.io/ysong-chat/flask-app:2a728c5-01ef91e4
        ports:
        - containerPort: 8080
        env:
          - name: PORT
            value: "8080"
        resources:
          requests:
            memory: "2Gi"
            cpu: "3"
            ephemeral-storage: "5Gi"
#            nvidia.com/gpu: "1" # Requesting 1 T4 GPU
          # limits:
          #   memory: "2Gi"
          #   cpu: "3"
          #   ephemeral-storage: "5Gi"
#            nvidia.com/gpu: "1" # Limiting to 1 T4 GPU